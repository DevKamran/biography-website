{
  "name": "portfolio-monorepo",
  "private": true,
  "workspaces": [
    "apps/agent"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=apps/agent",
    "build": "npm run build --workspace=apps/agent",
    "build:web": "npm run build --workspace=apps/agent",
    "start": "npm run start --workspace=apps/agent",
    "start:web": "npm run start --workspace=apps/agent"
  },
  "engines": {
    "node": ">=18.18.0"
  }
}
